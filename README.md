# laravel-deploy-script
A Bash script to automate the deployment of any Laravel application from zero to finish on a Linux server. Handles cloning, database setup, Apache config, SSL, permissions, and dependencies.
